# This repo is the agent-neutral SOURCE of the collab-board toolkit.

# Installed tooling (produced from skill/ by install.mjs — install per project instead)
.claude/

# Runtime collaboration session data (created by /collab-new in a target project)
.collab-board/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
