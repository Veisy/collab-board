# Collab-Board Catalog

<!-- One row per session. Reconciled by `activate`/`advance`/`terminal`/`reset`; lint (L16) only
     WARNs when a row disagrees with HEAD - it reports drift, it does not repair it. -->

| Session | Type | Status | Phase | Updated | Link |
|---------|------|--------|-------|---------|------|
